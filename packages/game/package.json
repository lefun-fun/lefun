{
  "name": "@lefun/game",
  "version": "3.0.0-alpha.4",
  "description": "Main dependency for writing game logic at https://lefun.fun.",
  "author": "Simon Lemieux",
  "repository": {
    "type": "git",
    "url": "https://github.com/lefun-fun/lefun",
    "directory": "packages/game"
  },
  "license": "MIT",
  "type": "module",
  "main": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rm -rf ./dist",
    "compile": "esbuild src/index.ts --bundle --format=esm --outdir=dist/esm --sourcemap --external:@lefun/core && tsc --emitDeclarationOnly",
    "watch": "esbuild src/index.ts --bundle --format=esm --outdir=dist/esm --sourcemap --external:@lefun/core --watch",
    "test": "vitest run src"
  },
  "devDependencies": {
    "@lefun/core": "workspace:*",
    "@types/lodash-es": "^4.17.12",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "peerDependencies": {
    "@lefun/core": "workspace:*"
  },
  "dependencies": {
    "immer": "^11.1.8",
    "lodash-es": "^4.18.1"
  }
}
